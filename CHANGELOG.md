# Changelog

## Unreleased

- Add fenced Windows Scenario execution with explicit setup, bounded payloads, interactive launch, exact Session recovery and stop, and hash-verified Evidence Bundles.
- Add the first public `windows check --target <file> --json` command for bounded, read-only Windows host inspection.
